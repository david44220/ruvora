import { EventsPage } from "@/components/public-pages";
export const metadata = { title: "Events", alternates: { canonical: "/events" } };
export default function Events() {
  return <EventsPage />;
}
